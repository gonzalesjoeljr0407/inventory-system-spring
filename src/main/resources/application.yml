spring:
    datasource:
        url: jdbc:mysql://localhost:3306/inventory?useSSL=false
        username: root
        password: root
    jpa:
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5Dialect